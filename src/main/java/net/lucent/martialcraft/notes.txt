-State
    -creates a state instance that stores data about that state(for example cancel data etc)
    -holds a list of stateChangeConditions, only one per possible state is allowed
    -first evaluates stateChangeConditions, then runs the state

-State Layer
    -holds a specific superclass of state, and can only accept states of that superclass
    -responsible for ticking and providing the relevant context window to said State Layer
    -even if a state does not have an internal connection to another, they can be forcibly changed
-Entity State
    -a capability that an entity can hold, used to interface with the state machine.
    -if a non-existent state layer is queried it should be created


-Default state layers
    -Locomotive
        -responsible for everything motion, should be functional for every entity, so does not read inputs directly, instead handles some
        sort of intermediate data, or just create a player and mob version, for example have a generic jump state, which has a player jump and mob jump superclass.
        we cannot just rely on velocity data, since it does not explain what caused that velocity (e.g player was knocked airborn but is not in a jumping state but launched state. simple velocity would not know that)
        this could still work however, by treating launched and states like it as "external" where the only way to have it applied is through external means,
        for now we will use reactive locomotive state.
        (so the state receives an input vector, this is the player inputs. launched for example would ignore this vector, while idle would move to another state)
        -states like grounded,airborne or swimming are handled through superclasses
    -Action
        -this will behave as a generic non movement state layer, for things like sword swings etc. most situations will only need 1 action layer,
         but some scenarios (like a dual wielding system) may want to add 2 action layers


Consider making it a requirement that a StateLayer receives a State Set (aka id-> state of all possible states a layer can be in)
This would be an interface and in most scenarios would be a wrapper around a registry(would also mean less space instead of a copy of every state to every stateLayer Instance)

most of the time not needed, but is important if we want any states to be "savable" and also for networking


For State Connection /Change have an event called gatherChangeStates? this can be called whenever but ideally in the constructor of the state directly OR on server starting or smth?
Probably should handle it On registry finished loading? i think this is where FML loading and stuff comes in

consider separating a state change/connection from the state and instead create a connection object that contains origin, target and changeCondition.
these would still be built the what my StateChangeConditionsHolder works, but will have one holder that builds a holder for each state.

this will then be handled through a StateConnectionProvider

Does not NEED a registry, HOW the Provider is created is not defined, however i would probably do it as a wrapper around a static object.
this is because each entity receives a fresh instance of a state layer.

so you might create a map of Entity type -> Provider
then run some event that is like create for entity type. and this would include some generic and some type specific connections.
this all gets wrapped in a static/constant object that the provider can access

(ok this might make me change my code AGAIN.

OK SO CREATE A STATE MACHINE, this takes ins a StatesProvider, StateConnectionProvider
this is ideally used as a static/constant


Also allows for more diversity, you can very easily limit the available states for different implementations


the State Machine does not directly store all valid states, this is because not all states are applied/handled through internal connection conditions (yet at least)
ideally all states are handled through conditions however states like stun, are easier to handle in an abstract condition
(however you can still make it work, by having some flag on the entity and if the flag is present change to stun, if the flag is not present remove stun)

the reason this works is because say we have 2 versions of stun, a fire stun and frozen stun. they both block the players movement, which can be handled through a generic
movement blocked state, then the extra effects of these 2 states, which are not relevant to locomotion, can be handled by an effect system.